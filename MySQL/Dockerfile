FROM mysql:8.0

ENV MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
ENV MYSQL_DATABASE=${MYSQL_DATABASE}
ENV MYSQL_USER=${SPRING_DATASOURCE_USERNAME}
ENV MYSQL_PASSWORD=${SPRING_DATASOURCE_PASSWORD}

EXPOSE 3306